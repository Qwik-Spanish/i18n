import { component$ } from '@builder.io/qwik';
import { ChangeLocale } from './change-locale';


export default component$(() => {

  return (
    <header>
      <ChangeLocale />
    </header>
  );
});
